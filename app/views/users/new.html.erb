<% provide(:title, t("user.account_create")) %>
<% provide(:button_text, t("ok_button")) %>
<% if logged_in? && current_user.admin? %>
  <h1 class="center">
    <%= t("user.account_create") %>
  </h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
