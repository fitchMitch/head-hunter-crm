<% provide(:title, t("person.add")) %>
<% provide(:button_text, t("add_button")) %>
<% if logged_in? && current_user.admin? %>
  <h1 class="center">
    <%= t("person.add") %>
  </h1>
  <div class='row'>
    <div class="col-md-3 hidden-sm hidden-xs"></div>
    <div class="col-md-6  col-sm-10 col-sm-offset-1 col-xs-12">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
