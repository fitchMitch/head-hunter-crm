<% provide(:title, t("person.edit")) %>
<% provide(:button_text, t("save_button")) %>
<% provide(:button_see_text, t("person.save_and_see_button")) %>
<% if logged_in? && current_user.admin? %>
  <h1 class="center">
  <%= t("person.edit") %></h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
