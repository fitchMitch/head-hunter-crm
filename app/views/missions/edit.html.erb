<% provide(:title, t("mission.edit")) %>
<% provide(:button_text, t("save_button")) %>
<% if logged_in? %>
  <h1 class="center">
  <%= t("mission.edit") %>
</h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
