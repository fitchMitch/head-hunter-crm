<% provide(:title,t('mission.create')) %>
<% provide(:button_text,t('add_button')) %>
<% if logged_in? %>
  <h1 class="center">
    <%=t('mission.create') %>
  </h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
