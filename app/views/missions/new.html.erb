<% provide(:title, 'Ajouter une mission') %>
<% provide(:button_text, 'Ajouter') %>
<% if logged_in? %>
  <h1 class="center">Ajouter une mission</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'mission_form' %>
    </div>
  </div>
<% end %>
