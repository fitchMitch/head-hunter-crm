<% provide(:title, t("mission.list") ) %>
<h1 class="center">
  <%= t("mission.list") %>
</h1>

<%= will_paginate %>

<%= render 'filter' %>
<%= render 'header_table' %>
<%= render @missions %>
<%= will_paginate %>
