<% provide(:title,t('job.add')) %>
<% provide(:button_text,t('add_button')) %>
<% if policy(@job).new? %>
  <h1 class="center">
    <%=t('job.add') %>
  </h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
