<% provide(:title, t("job.add")) %>
<% provide(:button_text, t("add_button")) %>
<% if logged_in? %>
  <h1 class="center">
    <%= t("job.add") %>
  </h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'job_form' %>
    </div>
  </div>
<% end %>
