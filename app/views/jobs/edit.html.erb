<% provide(:title, 'Editer ce poste') %>
<% provide(:button_text, 'Sauvegarder') %>
<% provide(:action, edit_job_path) %>
<% if logged_in? %>
  <h1 class="center">Editer un poste</h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'job_form' %>
    </div>
  </div>
<% end %>
