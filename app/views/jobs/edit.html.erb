<% provide(:title, t("job.edit")) %>
<% provide(:button_text, t("save_button")) %>
<% provide(:action, edit_job_path) %>
<% if logged_in? %>
  <h1 class="center">
    <%=  %>
  </h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
