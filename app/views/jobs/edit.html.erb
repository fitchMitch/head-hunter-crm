<% provide(:title, 'Editer ce contact') %>
<% provide(:button_text, 'Sauvegarder') %>
<% if logged_in? && current_user.admin? %>
  <h1 class="center">Editer un contact</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
