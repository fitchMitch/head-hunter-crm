<% provide(:title, t("company.add")) %>
<% provide(:button_text,  t("add_button")) %>
<% if logged_in? %>
  <h1 class="center">
  <%=  t("company.add") %>
</h1>
  <div class='row'>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
