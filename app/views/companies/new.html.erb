<% provide(:title, 'Ajouter une société') %>
<% provide(:button_text, 'Ajouter cette société') %>
<% if logged_in? %>
  <h1>Ajouter une société</h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  </div>
<% end %>
