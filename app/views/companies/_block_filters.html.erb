<div class= 'row spaceDown'>
    <div class='col-xs-2'></div>
    <div class="col-xs-10">
        <div class="btn-toolbar" role="toolbar" aria-label="...">
            <div class="btn-group btn-group-sm" role="group" aria-label="...">
                <button class="btn btn-default"> <%= fa_icon('filter') %> </button>
            </div>
            <div class="btn-group btn-group-sm" role="group" aria-label="...">
                <%= link_to  comactions_path(filter: 'unscheduled'), class: "btn btn-default " do %>
                <%=t('comaction.filters.with_no_comaction') %>
                <% end %>
                <%= link_to  comactions_path(filter: 'future'), class: "btn btn-default" do %>
                    <%=t('comaction.filters.now_and_after') %>
                <% end %>
                <%= link_to  comactions_path, class: "btn btn-default" do %>
                    <%= fa_icon('undo') %>
                <% end %>
            </div>
        </div>
    </div>
</div>
